# Task Tracker Configuration

# Port to expose task-tracker on (when not using Traefik)
TASK_TRACKER_PORT=8080

# Domain for Traefik routing (only used when traefik profile is enabled)
TASK_TRACKER_DOMAIN=task.local.example.com

# Watchtower update check interval in seconds
# 300 = 5 minutes (recommended)
# 60 = 1 minute (more frequent)
# 900 = 15 minutes (less frequent)
WATCHTOWER_POLL_INTERVAL=300

# Enable/disable optional services using profiles
# Separate multiple profiles with commas
# Available profiles: traefik, watchtower
# Examples:
#   COMPOSE_PROFILES=traefik,watchtower  (enables both Traefik and Watchtower)
#   COMPOSE_PROFILES=traefik             (enables only Traefik)
#   COMPOSE_PROFILES=watchtower          (enables only Watchtower)
#   COMPOSE_PROFILES=                    (disables both, use direct port access)
COMPOSE_PROFILES=
